# Ecosystem

The standard library ends where the ecosystem begins. Most applied
Rust is a thin layer of your logic over community crates: argument
parsing, serialization, async runtimes, and error reporting. The
[walkthrough](ecosystem/cli_json.md) combines two of them into one
small program; the chapters it builds on are linked from there.
